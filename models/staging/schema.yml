
version: 2

sources:
    - name: staging
      #For bigquery:
      database: data-talks-de-zoom-camp
      schema: trips_data_all

      # loaded_at_field: record_loaded_at
      tables:
        - name: green_tripdata
        - name: yellow_tripdata
         # freshness:
           # error_after: {count: 6, period: hour}